// let c =['hey','keyboard','mouse','kim']
// filter out words which have 'ke'in them

let c = ['hey', 'keyboard', 'mouse', 'kim'];

let result = c.filter(x => x.includes('ke'));

console.log(result);